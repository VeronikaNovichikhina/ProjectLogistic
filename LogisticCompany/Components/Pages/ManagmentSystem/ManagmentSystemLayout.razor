@page "/managment-system"
<div class="page-container">
    <div class="container-fluid">
        <!-- Header -->
        <div class="page-header glass-card">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <div class="d-flex align-items-center">
                        <div class="header-icon me-3">
                            <i class="fas fa-users-cog"></i>
                        </div>
                        <div>
                            <h1 class="page-title">Управление компанией</h1>
                            <p class="page-subtitle">Администрирование персонала, клиентами , заказами компании</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 text-end">
                    
                    <NavLink class="btn btn-primary btn-lg shadow-sm" href="/employees">
                        <i class="fas fa-plus-circle me-2"></i>Просмотр сотрудников
                    </NavLink>
                    <hr />
                    <NavLink class="btn btn-primary btn-lg shadow-sm md-10" href="/admin-home">
                        <i class="fas fa-plus-circle me-2"></i>Просмотр панели менеджера
                    </NavLink>
                </div>
            </div>
        </div>
    </div>
</div>
<style>
    .page-container {
        background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 50%, #ffffff 100%);
        min-height: 100vh;
        padding: 2rem 0;
        font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    }

    /* Header Styles */
    .page-header {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 2rem;
        border: 1px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    .header-icon {
        width: 70px;
        height: 70px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 18px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        color: white;
        box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
    }

    .page-title {
        font-size: 2rem;
        font-weight: 700;
        color: #2c3e50;
        margin: 0;
    }

    .page-subtitle {
        color: #6c757d;
        margin: 0.5rem 0 0 0;
        font-size: 1.1rem;
    }
</style>